# Rebuild ledger — one entry per item

**What is still to do is in `NEXT.md` beside this file.**

Reconstruction of the manuscript as a recalibration paper. Theory at the centre;
non-identification demoted from thesis to the reason for the ordering. Nothing is
deleted from the repository; material leaving the body moves to the supplement
with its numbers intact.

Status values: DONE, IN PROGRESS, BLOCKED, NOT STARTED.

| item | status | decision / finding | artefact |
|---|---|---|---|
| K1a Chronos analytic rebuilt from prose | DONE, **R14** | **FAILS.** Stored analytic series is one quantisation bin above the library's own decoder, on 4 asset×checkpoint combinations. Adjudicated at top_k=1 with no Monte Carlo error, 12/12. Registered as R14 (R11 slot taken); new pattern, and PROTOCOL.md Rule 2 gains the tolerance subsection. The announced validation was **blind, not coarse**: a uniform support translation leaves predictive std exactly invariant, and the coverage check ran on 40 dates at α=0.01 (grid 1/40). | `analysis/k1_verify/k1a_*`, `analysis/phase0/retracted_hypotheses.md` |
| K1b1 π̂ for TimesFM 2.5, Moirai 2.0 | DONE | VERIFIED to 6 dp from returns + series. Printed figures are the **cell mean**, not the pooled panel rate (0.0147 / 0.0182); the manuscript must say which, once. | `analysis/k1_verify/k1b1_*` |
| K1b2 distinct values at default top_k | DONE | VERIFIED on the 40-cell subsample: exactly 50 in 40/40. Controls fire at top_k=10 (→10) and 4094 (→331–648). Packaged defaults are top_k=50, top_p=1.0, temp=1.0, num_samples=20. | `analysis/k1_verify/k1b2_*` |
| K1b3 n₁₁ = n₁₀ = 0 vs code's CC pass | DONE | Percentages 34.6 / 53.5 VERIFIED. **Stated mechanism false**: zero pairs have n₁₁ = n₁₀ = 0; all 108 (and 167) are n₁₁ = 0 < n₁₀. "Too few exceedances to populate a transition table" must be rewritten. | `analysis/k1_verify/k1b3_*` |
| K1c CAViaR-AS 15/24 | DONE | VERIFIED. Second implementation agrees on the count and on the per-asset vector, 24/24, both windows. Task premise ("unverified") was out of date. | `analysis/k1_verify/k1c_*` |
| K1d evidence grade | DONE (1 BLOCKED) | `tab_baselines` NOT_EMITTED; six TSFM series UNVERIFIABLE_HERE; GARCH-N data-revised on 24/24. "Table I.6" has no referent — BLOCKED, and both candidates are panel-independent anyway. | `analysis/k1_verify/RESULTS.md` |
| K0a q̂_V vs uMCB | DONE | q̂_V is a rewriting of the **argmin** δ*, not of uMCB (the value). ρ = 0.993 on the calibration window, **0.52 on the evaluation window among well-specified series**. Conclusion narrowed: **q̂_V does not support an out-of-sample ordering between forecasters** — this says nothing about δ*. §6 writes q̂_V as measurement, not statistic. Abstract unchanged. AE-3 verified: in-sample improvement / uMCB = 0.99987 median. Independently reproduces `\nDegradedStatic` = 66. | `analysis/k0a_mcb/VERDICT.md` |
| K0b Escanciano & Pei and the precedent | DONE | Precedent is **specific** — to HS/FHS estimators and to the unconditional test; their own Lemma 1 gives a *consistent* weighted backtest, running opposite to Prop 6.1. §8 keeps δ*. §2 gains four attributions; their optimal weight w* **is** the manuscript's u_t. | `analysis/k0b_precedent/VERDICT.md` |
| **R14 registered** | DONE | new pattern: a check calibrated on a statistic the defect leaves invariant. Second instance in this project (first: the −3.500 band edge blocking 0 of 312 cells). `PROTOCOL.md` Rule 2 gains "The tolerance is part of the check". | `analysis/provenance/PROTOCOL.md`, `analysis/phase0/retracted_hypotheses.md` |
| **corrected analytic figures** | PENDING REGEN | π̂ 0.0175→**0.0173**, 0.0178→**0.0177**; ratios 1.750→**1.733**, 1.781→**1.772**; π̂(α=0.10) 0.1036→**0.1027**, 0.0989→**0.0981**; `\nKupMiniAnalyticTen` 22→**20**. Kupiec at α=0.01 unchanged at 8/24. QS −0.09%, unchanged at printed precision. Enter wherever they appear, including what stays in the body after the Chronos exhibit moves to the supplement. | `analysis/k1_verify/k1a_impact.json` |
| **R14 panels regenerated** | **DONE** | Both analytic panels rebuilt from the weights with `centers[id - n_special - 1]`, the tokenizer's own decode. 24 files, 121,923 rows per checkpoint, on the shipped dates. Matches the independent K1a reimplementation to 1e-15; negative control (stored series unshifted) rejected. Every pre-measured corrected figure reproduces to six decimals: π̂ 0.0175→**0.0173**, 0.0178→**0.0177**, ratios 1.750→**1.733**, 1.781→**1.772**, `\nKupMiniAnalyticTen` 22→**20**. Old panels quarantined, not deleted. | `analysis/k1_verify/k1a_verify_rebuild.{py,json}`, `quarantine/2026-08-27_r14_one_bin_offset/` |
| **route 1 is not exact panel-wide** | **DONE, new finding** | The arithmetic rule `stored − binwidth·scale` holds on 99.47% of dates and fails on 645 of 121,923: where the bin the CDF crosses moves index by one, the offset is not one bin. The 200-date block R14 validated the rule on contains none of them. Route 2 was therefore the right route for reasons that only appear at panel scale. Aggregates unmoved. | `analysis/k1_verify/k1a_verify_rebuild.json` |
| **float32 CDF ties** | **DONE, measured** | Rebuilt in float64 after the float32 run showed the crossing bin is ambiguous on 0.03–0.045% of dates, always by exactly one bin and in both directions. Measured against a full float32 arm on 10 assets rather than asserted, so the precision change is not confounded with the map fix. R14's defect moved 100% of dates in one direction; this is unbiased noise three orders of magnitude rarer. | `analysis/chronos_sampling/build_analytic_series.py --dtype` |
| **DQ panel gains a producer** | **DONE** | `analysis/phase3/dq_panel.csv` was read by three macros and written by nothing. Transcribed from `PREREG_DQ.md`, it reproduces all 312 committed cells to 1e-12 and returns the pre-registered 81.7% / 53.8%. After R14 the 48 analytic cells move by up to 0.074 in p-value with **zero rejection flips**, so `SeqDQRej*` are unchanged. | `scripts/build_dq_panel.py` |
| **L1 ten tables diagnosed** | **DONE, diagnosis** | Two reproduce (float repr only); two were hand-edited after generation, `git log -S` showing neither emitter ever wrote the shipped string; two had the superseded emitter wired in `make.sh`; three are pre-correction vintages of 2026-05-31, never regenerated after the 17 August sign correction, and **both are printed in the submitted `main_R1.tex`**; one is an unseeded LightGBM. | `analysis/provenance/L1_TABLE_REPRODUCTION.md` |
| **Moirai-1.1 counted twice** | **DONE** | `all_results.csv` has carried Moirai-1.1 since 17 August, and five scripts still concatenated the stale `moirai11_*results.csv` on top: its count columns came out at exactly 2×. `build_table1_r2.py` had documented the hazard and guarded against it; the guard is now in all five. | `rebuild_master_table.py`, `run_qV_ranking.py` (×2), `run_frontier.py`, `run_violation_rates.py` |
| **GBM-QR seeded** | **DONE** | `feature_fraction`, `bagging_fraction` and `bagging_freq` set with no seed: 58 of 216 rows changed their violation count between lightgbm 4.6.0 and 4.7.0. Seeds declared, `deterministic=True`; two independent runs now byte-identical. Cause inferred, not demonstrated — proving it needs 4.6.0 installed. | `Quantlets/CO_gbm_qr/baseline_gbm_qr.py` |
| **the gate count was a literal** | **DONE** | `paper_numbers.py` guarded on a `verdict` column `PROMOTION_GATE.csv` does not have, so the branch never ran and the `else` supplied `GateBlocked, GateSeries = 4, 13` as literals — in the one script whose purpose is that no figure is hand-carried. The fallback was right, which is why it survived. Now computed from `PASS`, and a missing column is an error rather than a default. | `scripts/paper_numbers.py` |
| **guard 4, and the pattern** | **DONE** | The build now fails when `PROTOCOL.md` or `MIGRATION.md` names a file git does not carry. Its control fires; on first run it caught the real instance — three of the four audits and `build_guards.py` itself. Nothing had ever invoked `build_guards.py`, so `make.sh` gains a `guards` target, and its all-or-nothing toolchain gate became per-guard: a guard that cannot run reports SKIPPED, not a pass. PROTOCOL gains the four modes in which a check stops being evidence. | `scripts/build_guards.py`, `make.sh`, `analysis/provenance/PROTOCOL.md` |
| **§7 counts moved with R14** | **DONE** | Zone upgrades 205→**204** rolling and 182→**181** static; "72 of the 205 are also deteriorations" → **71 of 204**. The 174 deteriorations and the net cost of 9 stand. | `analysis/ae_point4/`, `analysis/k2_indication/` |
| **repo moved out of iCloud** | **DONE** | The tree lived in `~/Library/Mobile Documents/com~apple~CloudDocs/`, and an iCloud sync renamed `main_R2.tex` and `main_R2.aux` to inode-numbered files mid-write, which is what deleted the manuscript on 27 August. Moved to `~/dev/cfp-llm-var` and pushed to a private remote; `origin` still points at the public QuantLet repository and was not touched. Venv, audits and guards verified from the new path. | `quarantine/2026-08-27_icloud_rename_incident/` |
| **a published number with no producer** | **LOGGED, 4th instance** | "1.041 against 1.038 in units of realised volatility" in §4.4 reproduces from no normalisation in the archive --- not the full-sample sd, not the 512-day context sd. The claim was removed rather than guessed at. Fourth of its kind after Table S.10, the two hand-edited `.tex` files, and `tab_master_results.tex`. | `analysis/chronos_sampling/analytic_validation_SP500.csv` |
| **the §4.4 validation was blind twice over** | **DONE** | Both announced agreements are now reported with the defect they cannot see. A one-bin translation leaves the predictive standard deviation *exactly* invariant, so the dispersion tolerance is on a quantity the failure mode does not move; and on 40 dates at α = 0.01 the violation rate lives on a grid of 0.025, so "agreement to four decimal places" means both routes returned the same multiple of that grid --- a coincidence guaranteed, not obtained. Cleanest instance of PROTOCOL's second mode, and it sits in the section offering the remedy. | `main_R2.tex` §4.4 |
| **`analytic_quantiles.py` carried R14 too** | **DONE** | A second, unrepaired copy of the wrong token-to-bin map, in the script that produced the very validation figures §4.4 cites. The announced agreement was therefore measured on the defective support it was meant to check. Fixed and re-run: dispersion agrees to 0.55%, not 0.3%. | `analysis/chronos_sampling/analytic_quantiles.py` |
| **check 5: N-of-M claims** | **DONE** | `audit_structural_claims.py` matched **one sentence shape**, so it saw neither "eight of them need nothing but the series" (lower case, different phrasing) nor "thirteen of the sixteen forecasters" --- the second mode of its own taxonomy, in the instrument written for that class. New check 5 flags every "N of M" whose numbers are both typed and undeclared; on first run it caught nine, of which **two were wrong**: the R-bar claim, and "16 of 20 DM pairs remain significant" against a panel that is 30 pairs with 30 significant and zero reversals. | `scripts/audit_structural_claims.py`, `analysis/provenance/DECLARED_COUNTS.tsv` |
| **a claim that inverted, not just drifted** | **DONE** | The supplement said bootstrap $p$-values are "uniformly more conservative than asymptotic counterparts". True for the pooled Kupiec statistic (GJR-GARCH 0.029 → 0.234) and **false** for the DM comparisons, where the bootstrap leaves 30 of 30 significant against 29 asymptotically. Rewritten: clustering costs the coverage test its rejection and costs the score comparisons nothing. | `supplement.tex`, `wild_cluster_dm.csv` |
| **Phase 2: the benefit measure** | **DONE, result** | Pre-registered with both interpretations written in advance; the outcome was the third branch, the two alternatives disagreeing. The 20 upgrades the deployable rule gives up are **20 of 20** by distance to nominal and **9 of 20** by quantile score, the other 11 being pairs the correction would have made worse on the score. Net cost 9 by the only measure that is not a function of the violation count. | `analysis/k2_indication/{PREREG_BENEFIT,RESULTS_BENEFIT}.md`, `benefit_measures.json` |
| **the zone is a strict coarsening** | **DONE, unanticipated** | Over 312 cells: **0** pairs are upgraded by the traffic light without moving closer to nominal, and **70** move closer without any zone change. The zone never disagrees in the other direction, so it is not a second opinion on the benefit but a coarser reading of the same movement, discarding 70 of the 274 improvements. Proposition 5.1 measured on the panel rather than proved in the limit. | `analysis/k2_indication/benefit_measures.json` |
| **the 30.9% question** | **ANSWERED** | Neither unexercised check enters it. `UndGateNow` is exactly `und` at the band's **upper** edge −1.8 in `band_sweep.csv`; the sweep varies only that edge, the lower edge −3.5 blocks 0 of 312 cells, and tail reach is a gate check that is not part of the band at all. The figure is unaffected by both. | `analysis/phase2/band_sweep.csv` |
| **`phase2_numbers.json` had no producer** | **DONE, 5th instance** | It feeds the Table 2 macros, and `paper_numbers.py` says it is emitted by three scripts: two write nothing of the kind and the third does not exist. New emitter rebuilds **19 of 37** entries from `delta_by_class.json`, `band_sweep.csv`, `pair_report.json` and `panel_scale_ratios_by_asset.csv`, all reproducing exactly; the remaining 18 are printed as unbacked rather than silently omitted. | `analysis/phase2/emit_phase2_numbers.py` |
| **the 18 unbacked numbers** | **DONE, all resolved** | Every one of the 18 entries `phase2_numbers.json` could not account for is **printed in the paper**. None was removed for want of an object: 5 came from `tab_bound_validation.csv`, 2 were already recomputed from `gap_ablation.csv` further down `paper_numbers.py`, 2 Fisher $p$-values compute from the 2×2 tables their own sentence prints, 3 belong to the constructed pair and the GJR comparison, 2 derive from the cell distribution, 1 is the measured Chronos rate under another name. Only `UndTight` has no object at all. | `scripts/paper_numbers.py` |
| **three published numbers were wrong** | **DONE** | $\hat\rho$ reaches **0.62**, not the 0.67 printed --- a value predating the sign correction. The bound's guaranteed floor is **85.2\%**, not "near 88\%", which was the maximum. The constructed pair's alternative threshold is **1.46σ**, not 1.32σ: it holds 56\% of the honest capital, not "barely half", and 1.32 appears to have been chosen so that "half" came out exact. | `tab_bound_validation.csv`, `analysis/phase2/pair.npz` |
| **UndTight is bracketed, not guessed** | **BLOCKED, declared** | The understatement at the tightened band edge cannot be recomputed: nothing in the repository writes `band_sweep.csv` and the machinery that would evaluate an intermediate edge is absent. Linear interpolation between the neighbouring rows returns 25.57, which is what was printed --- close enough to be the method, not close enough to assert it. Reported as the bracket 23.3–27.1\% with the reason in the sentence. | `analysis/phase2/band_sweep.csv` |
| **DM statistic recomputed under a stated convention** | **DONE** | $t = 0.399$, $p = 0.69$ reproduce from nothing here and the convention behind them was never recorded. Recomputed as cross-sectional mean per date, Bartlett kernel at 8 lags: $t = -0.98$, $p = 0.32$. The verdict is unchanged --- the two GJR variants are not distinguishable --- and the statistic is now one a reader can recompute. | `scripts/paper_numbers.py` |
| **Proposition: zone ⊂ coverage improvement** | **DONE, new statement** | Raised from a Section 7 note to a proposition in §5, beside the limit result it follows from. Every traffic-light boundary sits above nominal, so a downward crossing must move toward nominal, while movement interior to a zone is invisible: the implication holds and its converse fails. Measured on the panel --- 204 upgrades, 274 pairs closer to nominal, **0** upgraded without moving closer, 70 closer with no zone change. Zero counterexamples in 204 is the inclusion the proposition forces, not a correlation. This is the bridge between §5 and §7. | `sections/sec5_montecarlo.tex`, `benefit_measures.json` |
| **a number fitted to its own sentence** | **LOGGED, 5th pattern** | The constructed pair was printed at 2.61σ and 1.32σ, "differing by half". `pair.npz` gives **1.46σ** — 56% of the honest capital. 1.32 is the value at which the sentence's own word comes out exact. Every other defect in this register is a pipeline fault found by recomputing the object; this one would survive any amount of recomputation, because the pipeline never produced it. §8.6 now reads on 1.46σ and 56%, which is the same result. | `analysis/provenance/PROTOCOL.md`, `analysis/phase2/pair.npz` |
| **`band_sweep.csv` is reproducible after all** | **DONE, and it unblocks UndTight** | It had no producer and feeds the 30.9% of Table 2, so §8's headline appeared to rest on an orphan. It does not: `und(edge) = 100(1 − \|edge\|/\|q_true\|)` and δ solves `G_ppf(δ + α(1−2δ)) = edge`. Every row reproduces to 1e-10. The tightened edge −1.94 then gives δ = **0.017** and understatement **25.6%** — exactly Table 2's row — so the bracket introduced when the figure looked underivable is withdrawn in favour of the computed value. | `analysis/phase2/emit_band_sweep.py` |
| **the DM sign is weighting-dependent** | **DONE** | Equal weight on dates favours GJR-GARCH, equal weight on observations favours GJR-GARCH-t. The §9.2 claim that the ordering reverses between per-asset and pooled aggregation is verified and holds; the same reversal now appears a third time inside the DM statistic itself, and the sentence says so rather than resting on a direction that is an artefact of the weighting. | `main_R2.tex` §9.2 |
| **floor versus maximum** | **CHECKED** | The confusion appears once, in §4's theory passage, and is corrected there: the guaranteed floor is 85.2%, the 88.1% it was printed as being the maximum. No other site in either document states a coverage floor. | `sections/sec4_theory.tex` |
| **the supplement's 35 literals** | **DONE, 13 were wrong** | Guard 2's whole backlog closed. **13 of the 35 did not reproduce**, in five kinds: the tuned-GBM ablation is quoted on nine series against a grid of **thirteen** and a table on the same page reading 8/13 (88.9% is 8/9 and matches no count in the archive); the gate's resolution was printed as 2.1e-4, which is the **mean** of two cells quoted once as a minimum and once as a maximum, against **1.3e-4**; the score-persistence range was the **six-pair** bound-validation range attached to the **four-pair** ablation, and stale besides — the correct four-pair range is [+0.05, +0.46] and its macros already existed; six backtest p-values and one power figure had **no producer at all** — `construct_pair.py` stops at the LP and `sim.npz` holds 20,000 draws, not 500,000; and one control mean was rounded twice. Sixth and seventh artefacts of their kind. | `analysis/provenance/SUPPLEMENT_LITERALS.md` |
| **guard 2 could not read tables** | **DONE, and it was hiding two** | The guard replaced `\begin{tabular}...\end{tabular}` wholesale before extracting literals. Right for generated tables — `\input` is substituted earlier, so none reaches that point — and wrong for the six tabulars written by hand in the two documents. It cost **0.990 and 0.988**, the violation rates under the inverted-sign defect, sitting in a Section 8 row whose neighbouring column was already `\nRawPiTimesFM`. Now only the **column specification** is stripped, by brace-scanning, so `p{3.5cm}` stays out and rows come in; the control plants a literal inside a tabular that carries lengths, because a prose-only control cannot fail at the resolution of a table-only defect. | `scripts/build_guards.py` |
| **the pair is indistinguishable by theorem, not by simulation** | **DONE, reframed** | Recomputing S.8's p-values forced the question the paragraph never asked. Both laws place exactly α mass strictly below their own threshold, so the exceedance indicator is Bernoulli(α) and independent under **both** and every exceedance-path test has the *identical* null law — a property of the construction, not a simulation result. Six unremarkable p-values are the check that the sampled law reproduces its own LP constraints, and the pre-registration required the supplement to say which of the two it is doing. It now does. The ES-matched fifth constraint is feasible and Z₂ rejects at **0.090**, interval [0.081, 0.099], excluding the nominal size: "necessary, not sufficient" holds, on the pre-registered first branch. | `analysis/phase2/{PREREG_PAIR_BACKTESTS.md,pair_backtests.py}` |
| **a generated table contradicted itself** | **DONE** | `tab_h14_small_sample.tex` carried a note, hard-coded at `run_robustness_mc.py:416`, saying corrected rates at T ≤ 500 "exceed the nominal level", beside its own π̂ column of 0.007–0.008 against a nominal 0.010 and two pages from S.4.1 calling the same rates conservative. True under the plain empirical quantile, false under eq. (8), and it survived the re-run because it is a string in the emitter rather than a computed field. PROTOCOL's fourth mode, in generated prose. Re-running the full Monte Carlo reproduces `study1_small_sample.csv` **byte-identically** and h15/h16 unchanged, so the correction is the note alone. | `Quantlets/CO_robustness/run_robustness_mc.py` |
| **`--check` was rewriting its own reference** | **DONE, self-inflicted** | Reading the δ\* grid spacing by importing `delta_by_class` re-ran the module, and it writes `delta_by_class.json` at import — so `paper_numbers.py --check` refreshed the artefact it checks against and could never fail on a stale one. Introduced and caught in the same session; the defaults are now read with `ast` from the function signature, and the JSON is verified untouched across a `--check`. | `scripts/paper_numbers.py` |
| **check 5 skipped every half-macro claim** | **DONE, 2 of 11 were wrong** | `audit_structural_claims.py` dropped any "N of M" whose numerator *or* denominator was a macro, reading one artefact-backed side as evidence for both. **11 claims had that shape and none was ever read.** Two were wrong: the analytic Chronos-Small Kupiec vector is 5, **8**, 5, 4 across the four levels and was printed 5, **9**, 5, 4 — ASX200 sits at p = 0.059, just outside the edge, so the 9 is a pre-R14 vintage; and the rolling Christoffersen low group was printed 0--5 against a panel whose minimum is 2 and whose foundation-model range is **2--8**. The other nine verify exactly: 0/17 on Z₂, 19 and 14 on DQ, 16 raw Kupiec, 21 and 9/23 on the ES correction, 12 of 13 green everywhere, 10 the best rolling CC, 0 of 312 on the level convention. | `scripts/audit_structural_claims.py` |
| **a macro named for the opposite of what it holds** | **DONE, no printed error** | `tab_alpha_response.csv`'s `kupiec` column counts assets that **pass**, and is exactly 24 minus the rejection count in `all_results` at all four levels for both series. Every other Kupiec macro in the registry counts rejections. The prose already read "passing Kupiec on ...", so nothing printed was wrong — the trap was the name, in the same class as `\nGapVaRAlt` still holding 1.32. Renamed `\nKup*` → `\nKupPass*`. | `scripts/paper_numbers.py` |
| **a threshold I invented, caught by my own assertion** | **DONE** | Replacing "0--5" I wrote "the three series with R̄ above 0.35", a cut chosen after seeing the data, and guarded it with an assertion that the top three separate from the rest. **The assertion fired**: Lag-Llama is at 0.357 against 0.184 below it, a factor of 1.9, while the two truncated Chronos are at 17 and 24 — so the top-three grouping is not natural and the two-order gap sits *inside* it. Rewritten on the master table's own `kind` column: the 8 foundation-model series, 2 to 8. | `scripts/paper_numbers.py` |
| **PROTOCOL gains two rules and a second instance** | **DONE** | (i) **Where the negative control is planted** — in the region the check covers worst, not the most typical. Guard 2's control was in prose and the guard was blind to tables; check 5's was two typed numbers and the check was blind to half-macro claims. Both blind spots are the same configuration, **a literal beside a macro**, which is the worst in a document because it reads as verified. (ii) **The defect is in the instrument more often than in the object** — counted, 4 of 6 over two days. (iii) The fifth pattern has a **second instance**: 88.9% is 8/9 and matches no count in a 13-model archive, and the clause naming the object was itself the false part. | `analysis/provenance/PROTOCOL.md` |
| **K2 §6 What recalibration restores** | **DONE** | Opens on the result: coverage restored, \nMainGreen 335 of 384 green and Kupiec 123 → 273, and the corrected column cannot rank *because* of it. q̂_V written as a measurement in forecast units — the argmin, not the score reduction. Both rank correlations now from one subset (264 well-specified): 0.99 in-window, 0.52 across. The CC mechanism corrected: the table is populated in every degenerate case, one cell is empty, and the correction empties that cell more often, which is why 34.6% rises to 53.5%. Four narrator sentences removed with every fact kept, including the 78.4%. | `main_R2.tex` §6 |
| **the density is the mechanism, by a third route** | **DONE, both predictions falsified** | Pre-registered in both directions and both were wrong, oppositely. The proposal (density explains the shift-to-score collapse) fails: supplying each pair's f moves ρ from 0.531 to **0.529**, and the pre-registered circularity branch fires at ρ = 1.0000 because f is solved from uMCB. My objection (density cannot enter a shift-to-shift correlation) also fails: split at median f, ρ is 0.361 against 0.572. The algebra is right about the definition and silent about the sampling distribution — sd of a sample α-quantile is √(α(1−α)/n)/f. Tested as a functional form: slope **−0.940** (se 0.103) against a classical −1, 0.6 se away, and the median gap is **1.11×** the predicted sd with no fitted constant. | `analysis/k0a_mcb/RESULTS_DENSITY_MECHANISM.md` |
| **the density span was a cross-window quantity** | **DONE, 7th such figure** | 489 divides a test-window uMCB by a calibration-window q̂_V, carrying both windows' noise. Within one window the implied f spans **14.8**, an order of magnitude less, and still carries the sentence. `MEMO.md` and the K0a verdict both corrected; the verdict had also carried 481, which reproduced from nothing. | `analysis/umcb/MEMO.md` |
| **K2 §7 What it costs, and when** | **DONE** | Promoted to its own section. Four debts closed: the w = 125 forward reference, the indication rule as a consequence of Prop 5.3, the corrected ledger, and the 20 upgrades decomposed **in the text** — all 20 move closer to nominal, 11 pay for it with a worse score, so the net cost is **9**, not 20, on the only measure that does not read the exceedance count. The rule is better than its accounting in zones showed. | `main_R2.tex` §7 |
| **K4b: the w sweep, and it is not a variance curve** | **DONE, sharper than expected** | 312 of 312 cells on all three windows. The pre-registered separation at w = 125 half-holds: the shift is larger on 281/312 and the dispersion exceeds √2, but so does the 250→500 step, by more — implied exponent 0.62 then 0.97, against 0.5 for a variance curve. What separates the three is that **they do not estimate the same quantity**: k/w gives effective levels 1.0000, 0.9960, 0.9920 against a nominal 0.99, and the overshoot falls by a factor of five. The degenerate estimator at w = 125 is the extreme case of a confound present at all three, which is a better answer to R2-1 than the one the rebuild notes anticipated. No window recommendation follows, and the section says why. | `analysis/phase3_windows/RESULTS_W_SWEEP.md` |
| **K2 §8 Why structural validation precedes correction** | **DONE** | The ordering is derived, not asserted: the correction is indifferent to the forecaster, so validating after it tests nothing; a coverage test identifies only u_t, so validating before it does not help if the validation is a coverage test. Priority over "exceedance backtests are weak" is **explicitly disclaimed**. Two things separate the argument, neither being that observation: the **alternative set** — Escanciano–Pei's agree on the mean of u_t, Prop 8.1's on the whole process, so their consistent weighted backtest and this proposition coexist without conflict — and the **information set**, which is not a weaker claim but a different one: Gordy–McNeil and Kratz–Lok–McNeil establish what a supervisor holding the predictive distribution can do, and a firm consuming a third-party series holds thresholds and returns and cannot obtain a PIT value from a forecaster that published no distribution. δ* and `tab:gate_residual` stay. | `main_R2.tex` §8 |
| **four attributions enter the bibliography** | **DONE, one still uncited** | Escanciano–Pei, Gordy–McNeil, Kratz–Lok–McNeil cited in §8. Cont–Deguest–Scandolo is in the bib and **not yet cited** — it is §2's central framing debt, the estimation procedure as part of the risk measure. Gordy–McNeil carries **no page range**: the verdict records the volume and not the article number, and supplying one would be the eighth instance of the same pattern. | `calibrating_the_oracle.bib` |
| **K2 §9 Limitations** | **DONE** | Promoted to a section. Three subsections added. **The three absences of verdict in one sentence**: Christoffersen degenerate on 204 of 384, tail reach inapplicable on all 13 gated series, and the gate's own lower band edge blocking 0 of 312 — a statistic that cannot be computed, a check with no object, and a bound nothing approaches, all returning absence of evidence in a format read as adequacy, the third inside the paper's own gate. **The defect census as argument**: 4 of 6 defects were in a check, which is why a coverage test cannot carry validation alone — a validation instrument is an estimator and fails the same way, more often than what it validates. **The gate's limit in closed form**, not anecdote, with the out-of-sample evidence and what would overturn it stated. | `main_R2.tex` §9 |
| **verification produced a result, not only corrections** | **DONE, recorded in §9** | The effective-level overshoot was not derived and confirmed. It surfaced twice from opposite directions — a Monte Carlo control written to check a harness returned zero on a correct forecaster, and a window sweep run to answer a referee's request for a variance curve that turned out not to exist. Neither was looking for it. It was visible in the algebra of eq. (8) all along and was noticed only when two unrelated checks returned the same anomaly. | `main_R2.tex` §9 |
| **K2 §2 Related Literature** | **DONE, four attributions** | The delimitation paragraph now **opens** with the one that costs most: Escanciano–Pei's optimal weight w\*(I) = F_I(m_α(I,θ₀)) **is** the manuscript's u_t, eq. (11) — the same functional, opposite uses, they weighting by it to recover power inside the exceedance class and Prop 8.1 showing it exhausts what that class carries. The two coexist because the alternative sets differ, theirs on E[u_t] and this on the whole process. Gordy–McNeil and Kratz–Lok–McNeil enter as the class reading more than the indicator, with the reporting regime each requires stated. **Cont–Deguest–Scandolo (2010) enters as the central framing**, not a courtesy: the risk measurement procedure is the pair (estimation step, measure), and this paper's extension is narrow — the estimation step is a documented software default, unreported and invisible to every diagnostic the domain runs. And the multiplication-factor precedent: their own consistent test rejects portfolios whose capital multiplier sits at the 3.00 floor, reported in 2012. Register: zero "we"/"our" in the section, against three editorial uses before. | `main_R2.tex` §2 |
| §4.4 analytic estimator | NOT STARTED | moves to the supplement **with** the Chronos exhibit (K3); the corrected numbers still enter the body wherever the body cites them | |
| K2 §1 Introduction | NOT STARTED | | |
| K2 §2 Related Literature | NOT STARTED | after K0b | |
| K2 §3 Methodology, the shift convention | **DONE** | Not one stray variant: **52 sites, four conventions**. 23 ORDER_STATISTIC, 13 NOT_THE_SHIFT, 8 LEVEL_K_OVER_N, 5 PLAIN_QUANTILE, 3 CANONICAL. Measured on 312 pairs: LEVEL changes **0/312** violation counts; PLAIN changes **120/312** and moves cell-mean corrected π̂ from 0.010617 to 0.010989. Root cause: the convention was in a `cfp_config` docstring and implemented nowhere. Fixed by `cfp_config.conformal_quantile`, migrating the PLAIN sites, and `scripts/audit_qv_convention.py`, which **fails the build** on an undeclared site (verified: a planted fifth convention exits 1). Both universal claims in the manuscript corrected. | `analysis/provenance/QV_CONVENTION.md`, `QV_CONVENTION_SITES.tsv` |
| Tables S.5–S.10, S.9 regenerated | **DONE** | `run_robustness_mc.py`, `gap_ablation.py` and the package's `bootstrap.py` were on the plain quantile. Re-run under eq. (8). **Table S.10 also had no emitter** — it was hand-authored from a pre-correction vintage (ρ̂ = −0.15 vs +0.32; Moirai-2.0/NATGAS π̂ = 0.076 vs 0.0152) and now has one. The gap ablation's two printed conclusions are unchanged (0.0005, 0.0058). S.5–S.8 change materially and now agree with §5's independent grid. | `analysis/convention/` |
| **k ≥ n at short windows** | **DONE** | Exact: k ≥ n ⟺ n < 2/α − 1, so at α = 0.01 the shift **is the sample maximum** for every n ≤ 198 (78, 38, 18 at α = 0.025, 0.05, 0.10). R2-1's w ∈ {125, 250, 500} is therefore **not three points on a variance curve** — at w = 125 the shift is the window maximum. Stated in §3.2.1 and Supplement S.4.1. | `QV_CONVENTION.md` |
| §5 traffic-light result | **DONE** | Raised to **Proposition 5.1** with **Corollary 5.2**: the classification identifies sign(π − τ) in the limit and nothing else; at α = 0.01, τ = 1.6α, so any series cutting off up to 1.6× nominal is passed with probability → 1 — a VaR understated by 12.4% under standardised t₅ (16.3% under t₃, 7.8% Normal). Independent of δ*. Checked: identity exact 25/25 cells, normal approximation within 2.5 points, boundary control stuck at ~50%. | `analysis/k2_sim/check_prop_tl.py` |
| **OPEN: §7 must close a forward reference** | TODO | §3.2.1 now promises that §7 reports what w = 125 does. K4b must deliver it. | |
| K2 §4 Theory | **DONE** | Theory moved from Supplement S.9 into the body as §4, at full length: Assumption 4.1, Lemmas 4.2/4.4, Theorem 4.5, **Corollary 4.6 — newly stated**, Propositions 4.7/4.8, Remarks 4.3/4.9. The GARCH corollary previously had a proof and **no statement anywhere in either document**. Supplement S.9 reduced to proofs only; every S.9.x back-reference repointed and verified against its heading. | `sections/sec4_theory.tex` |
| K2 §5 Monte Carlo | **DONE** | Grid extended from 2 to 5 sample sizes, T ∈ {500…10000}, 12,500 replications. Published Table S.26 **reproduces exactly** from its per-replication artefact. All four pre-registered predictions hold, including the fourth: **the raw Basel classification diverges rather than converging** — Student-t(5), miscovering by 51%, is green 61.6% → **74.2%** as T grows twentyfold. New: `figures/fig_mc_convergence`. | `sections/sec5_montecarlo.tex`, `analysis/k2_sim/` |
| K2 §6 What recalibration restores | NOT STARTED | after K0a | |
| K2 §7 What it costs and when | NOT STARTED | | |
| K2 §8 Why structural validation precedes correction | NOT STARTED | after K0b; keeps δ* | |
| K2 §9 Limitations | NOT STARTED | | |
| **K3 moves to supplement** | **DONE** | Moved with numbers intact and pointers from the body, nothing deleted. The Chronos exhibit becomes **S.14** (1,293 words, five subsections) carrying both reasons §4.4's validation was blind — a one-bin translation leaves the predictive s.d. exactly invariant, and 40 dates put the violation rate on a grid of 0.025. The five failure modes become **S.15** as a pipeline-hygiene note. The TSFM-versus-GARCH panel needed no move: it is already S.12 and the body already disclaims the ranking. The body keeps a pointer paragraph in each place and, in §7, the sentence the plan reserves for it — the truncated series reaching π̂ = 0.0108 and 19 of 24 green. | `supplement.tex` S.14–S.15 |
| **the one-bin rule's 0.53%** | **DONE, written up** | The closed-form correction `stored − binwidth·scale` was validated on 200 re-run dates and agreed on all of them. Panel-wide it holds on **99.47%** of 121,923 dates and fails on **645**, where the bin the CDF crosses moves index by one. The 200-date block contains none of them — and under independence a block that size returns no failure about **35%** of the time, so the agreement observed is consistent both with the rule being exact and with it failing as often as it does. Same form as the other two blind agreements. The corrected panels come from the re-run, and now the reason is measured rather than preferred. | `analysis/k1_verify/k1a_verify_rebuild.json` |
| **three renumberings, three caught** | **DONE** | Moving two blocks out of the body renumbered §7 and §8's propositions from 8.1/8.2 to 7.1/7.2, orphaned four `Table~\ref{tab:failure_modes}` references, and left three supplement `\ref`s pointing into the body. Check 6 caught the propositions, guard 1 caught the table and the dangling refs, and `audit_supplement_targets` printed the target heading of every repointed reference so each could be read rather than assumed to resolve. | `scripts/audit_structural_claims.py` |
| K4a zero-shot vs fitted | NOT STARTED | | |
| K4b hyperparameters, w ∈ {125,250,500} | NOT STARTED | | |
| **§6 retitled, §1's map rewritten** | **DONE** | "The Audit" was a title from the architecture in which the audit was the thesis. After the failure modes moved to S.15 the section carries the panel and the ordering by R̄, so it is now "The Panel, and the Size of the Correction It Requires". §1's roadmap was stale in four places — it pointed at a Chronos section that had moved, an audit section that had been retitled, and one recalibration section that is now two, and a Discussion that is now Limitations plus Implications. Rewritten against the real thirteen, with the **three claims stated in the fixed order first** and every supplement pointer read against the heading it resolves to. | `main_R2.tex` §1, §6 |
| **line 1629 verified, not assumed** | **DONE** | "Cross-family score rankings are reported in Supplement~S.12" resolves to the generic heading "Secondary Tables and Figures", which is exactly the shape guard 1 passes on and the heading audit was written to expose. Checked the content rather than the pointer: S.12 inputs `tab_dm_pvalues`, whose CSV is 5 benchmarks × 6 foundation-model series — the rectangular cross-family panel of R3. The claim holds; no second instance of "verdict outlives its state". | `Quantlets/CO_quantile_scores/tab_dm_pvalues.csv` |
| K5 recounts and hygiene | IN PROGRESS | New audit `scripts/audit_supplement_targets.py`: the existing reference check passed on *resolution*, which is satisfied by a reference pointing at the wrong subsection — and that is what the renumbering produced. The new one prints the target heading beside the citing sentence. Orphan logged: `cfp_ijf_data/paper_outputs/tables/simulation_results.csv` (6 April) disagrees with the published table on every number and backs nothing. | `scripts/audit_supplement_targets.py` |
