# Focused novelty and mathematical audit

10 September 2026. Audit of the current R8, following the author's request.

Read the complete proofs of Proposition 4.1, Corollary 4.2,
Proposition 4.3 and Proposition 7.1, including assumptions and their
connections to the empirical estimator. Compare with primary sources on
dependent quantiles, pinball risk, extreme-order asymptotics, lower bounds,
pairwise independence and conformal selection. A targeted search cannot
establish priority or exhaust the literature.

The following deterministic checks use separate implementations; they
do not import existing numerical producers. They generate no random paths,
financial forecasts, model fits or new data:

1. Recompute the finite counterexample by a closed composition formula
   for renewal blocks, integrating rational polynomials exactly. Compare
   with the stored certificate produced by a recurrence.
2. Check the contiguous transfer on a nonreversible three-state chain,
   for all histories up to length five and four different static rules.
   This tests the inequality's algebra, not the density assumptions of
   the asymptotic proposition.
3. Check the fixed-level loss expansion against exact iid uniform order
   statistic moments, including ranks and local shrinkage factors.
4. Give an alternative proof of Proposition 7.1 using only histories with
   no positive refresh values. This bypasses Poisson approximation and
   the full Bayes functional limit. Check the positive limiting constant
   and the finite contiguous penalty on a fixed grid.

Each check has a deliberate incorrect expression that must be rejected.
Preserve all historical outputs and validation receipts. If manuscript
edits follow, use a separate current-source validation; do not weaken
historical hash checks. The audit is performed by the same AI assistant,
with independent derivations and code. It is not an external human review
or a formal proof-assistant certificate.
